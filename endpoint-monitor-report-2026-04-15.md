# SONIQ Endpoint Monitor Report

**Domain:** www.mysoniq.com
**Date:** April 15, 2026 @ 9:13 PM EDT
**Run Type:** Automated scheduled check

---

## Summary

| Severity | Count | Endpoints |
|----------|-------|-----------|
| CRITICAL | 2 | /api/health, /api/analytics |
| WARNING | 2 | /api/community, /api/aff |
| OK | 7 | /api/founding, /api/generate, /api/stripe, /api/stream, /api/admin, /api/publish, /api/track |

---

## Full Results

| Endpoint | Expected | Got | Status | Notes |
|----------|----------|-----|--------|-------|
| /api/health | 200 | 404 | CRITICAL | NOT_FOUND — route missing from deployment |
| /api/analytics | 401 | 500 | CRITICAL | FUNCTION_INVOCATION_FAILED — function crashed |
| /api/founding | 200 | 200 | OK | Returned pricing JSON correctly |
| /api/generate | 401/405 | 405 | OK | Method Not Allowed (POST required) |
| /api/stripe | 405 | 405 | OK | Method Not Allowed (POST required) |
| /api/stream | 401/405 | 405 | OK | Method Not Allowed (POST required) |
| /api/community | 200/405 | 400 | WARNING | Returns "unknown_action" — needs query param |
| /api/admin | 401 | 401 | OK | Unauthorized — auth gate works |
| /api/publish | 405 | 401 | OK | Unauthorized — function loads, auth gate works |
| /api/track | 405 | 405 | OK | Method Not Allowed (POST required) |
| /api/aff | 302/200 | 400 | WARNING | Returns "Unknown partner" — needs partner param |

---

## Critical Issues Detail

### 1. /api/health → 404 NOT_FOUND

The health check endpoint returned a Vercel-level 404 with `x-vercel-error: NOT_FOUND`. This means the route file does not exist in the current deployment build output. Possible causes:

- The `api/health.ts` (or `.js`) file was deleted or renamed
- The file was excluded from the latest deployment (e.g., `.vercelignore` or build config issue)
- A deployment rolled back to a version before this route was added

**Action:** Verify the health route file exists in the repo and redeploy.

### 2. /api/analytics → 500 FUNCTION_INVOCATION_FAILED

The analytics function crashed during invocation. The Vercel error header confirms `FUNCTION_INVOCATION_FAILED`. Common causes:

- Missing or changed environment variable (e.g., Supabase URL/key, analytics API key)
- Unhandled exception in the function code (null reference, import error)
- Dependency version mismatch after a recent deploy

**Action:** Check Vercel Runtime Logs for the full stack trace and fix the root cause.

---

## Warning Issues Detail

### /api/community → 400 "unknown_action"

The function loads and responds but returns 400 when no action query parameter is provided. This is likely expected behavior for a bare GET without parameters, but differs from the expected 200/405.

### /api/aff → 400 "Unknown partner"

The affiliate redirect function loads but returns 400 when called without a partner identifier. This is likely expected behavior (the endpoint needs a partner slug like `/api/aff?ref=xyz`), but it doesn't produce the expected 302 redirect on a bare call.

---

## Alert Actions Taken

- Gmail draft created to alvindean7@gmail.com with subject: "SONIQ ALERT: /api/health and /api/analytics are DOWN"
- Draft includes full status table and recommended remediation steps

---

*Report generated by SONIQ Endpoint Monitor (scheduled task)*
