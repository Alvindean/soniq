# SONIQ Endpoint Monitor Report
**Date:** April 10, 2026 — 07:08 UTC
**Domain:** www.mysoniq.com

---

## Summary: 2 CRITICAL | 3 WARNING | 6 OK

## Full Status Table

| Endpoint | Response Code | Expected | Response Time | Status |
|---|---|---|---|---|
| /api/health | 200 OK | 200 | ~1s | ✅ OK |
| /api/analytics | 502 → TIMEOUT | 401 | >30s (timeout) | 🔴 CRITICAL |
| /api/founding | 200 OK | 200 | ~2s | ✅ OK |
| /api/generate | 405 | 401/405 | ~2s | ✅ OK |
| /api/stripe | 405 | 405 | ~1s | ✅ OK |
| /api/stream | 405 | 401/405 | ~3s | ✅ OK |
| /api/community | 400 | 200/405 | ~2s | ⚠️ WARNING |
| /api/admin | TIMEOUT x4 | 401 | >30s (timeout) | 🔴 CRITICAL |
| /api/publish | 401 | 405 | ~1s | ⚠️ WARNING |
| /api/track | 405 | 405 | ~1s | ✅ OK |
| /api/aff | 400 | 302/200 | ~2s | ⚠️ WARNING |

## CRITICAL Issues

### /api/analytics — DOWN
- First attempt returned HTTP 502 (Bad Gateway)
- Three subsequent retries all timed out (server not responding)
- This endpoint is completely unreachable

### /api/admin — DOWN
- All four attempts timed out (server not responding)
- This endpoint is completely unreachable

### Likely Root Cause
The /api/health check revealed `SUPABASE_SERVICE_KEY: false`, meaning this environment variable is missing or empty in Vercel. Both /api/analytics and /api/admin likely depend on the service key for privileged database queries, which would cause them to hang or crash.

## WARNING Issues

### /api/community — Unexpected 400
- Returned: `{"error":"unknown_action","received":""}`
- Expected 200 or 405. The function loads but expects an `action` parameter even on GET.
- Low severity — likely by design (requires action query param).

### /api/publish — Returns 401 instead of 405
- Returned: `{"error":"unauthorized"}` with status 401
- Expected 405 (POST required). The function checks auth before method.
- Low severity — function is operational, just checks auth first.

### /api/aff — Returns 400 instead of 302/200
- Returned: `{"error":"Unknown partner"}` with status 400
- Expected 302 redirect or 200. Needs a partner identifier in the URL path or query.
- Low severity — function is operational, just needs a valid partner param.

## Health Check Details
```json
{
  "supabase": "ok (profiles: 0)",
  "redis": "ok (total_songs: 3)",
  "crypto": "ok",
  "SUPABASE_SERVICE_KEY": false  ← MISSING
}
```

## Recommended Actions
1. **IMMEDIATE:** Set SUPABASE_SERVICE_KEY in Vercel environment variables
2. **IMMEDIATE:** Redeploy after setting the key, then re-test /api/analytics and /api/admin
3. Check Vercel function logs for FUNCTION_INVOCATION_FAILED on both endpoints
4. Consider adding timeout handling in analytics and admin functions to fail fast instead of hanging

---
*Generated by SONIQ Endpoint Monitor — Scheduled Task*
