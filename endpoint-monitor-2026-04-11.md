# SONIQ Endpoint Monitor Report
**Date:** Saturday, April 11, 2026 — 07:07 UTC
**Domain:** www.mysoniq.com
**Monitor Run:** Scheduled automated check

---

## Status Summary

| # | Endpoint | Expected Code | Received Code | Response Time | Status |
|---|----------|--------------|---------------|---------------|--------|
| 1 | `/api/health` | 200 | **200** | ~1s | ✅ OK |
| 2 | `/api/analytics` | 401 | **TIMEOUT** | >30s (3 retries) | 🔴 **CRITICAL** |
| 3 | `/api/founding` | 200 | **200** | ~2s | ✅ OK |
| 4 | `/api/generate` | 401/405 | **405** | ~2s | ✅ OK |
| 5 | `/api/stripe` | 405 | **405** | ~2s | ✅ OK |
| 6 | `/api/stream` | 401/405 | **405** | ~2s | ✅ OK |
| 7 | `/api/community` | 200/405 | **400** | ~2s | ⚠️ WARNING |
| 8 | `/api/admin` | 401 | **TIMEOUT** | >30s (3 retries) | 🔴 **CRITICAL** |
| 9 | `/api/publish` | 405 | **401** | ~2s | ⚠️ WARNING |
| 10 | `/api/track` | 405 | **405** | ~2s | ✅ OK |
| 11 | `/api/aff` | 302/200 | **400** | ~2s | ⚠️ WARNING |

---

## Overall Verdict: 🔴 2 CRITICAL / ⚠️ 3 WARNING / ✅ 6 OK

---

## CRITICAL Issues (Immediate Attention Required)

### 1. `/api/analytics` — TIMEOUT / UNREACHABLE
- **What happened:** 3 consecutive fetch attempts all returned "connector server not responding" — the function either crashed (FUNCTION_INVOCATION_FAILED) or exceeded Vercel's execution timeout.
- **Impact:** Analytics data cannot be retrieved by the frontend. Dashboard analytics may be blank for all users.
- **Recommended action:** Check Vercel runtime logs for `/api/analytics`. Look for unhandled exceptions, Supabase query timeouts, or memory limits exceeded.

### 2. `/api/admin` — TIMEOUT / UNREACHABLE
- **What happened:** 3 consecutive fetch attempts all returned "connector server not responding" — same pattern as analytics.
- **Impact:** Admin panel is completely inaccessible. No administrative operations (user management, content moderation, etc.) can be performed.
- **Recommended action:** Check Vercel runtime logs for `/api/admin`. This endpoint likely shares the same root cause as `/api/analytics` (possible Supabase connection issue or heavy query).

---

## WARNING Issues (Non-Critical but Worth Investigating)

### 3. `/api/community` — Returned 400 instead of 200/405
- **Response body:** `{"error":"unknown_action","received":""}`
- **Assessment:** The function loaded and executed, but requires an `action` query parameter for GET requests. This is expected behavior for a parameterized endpoint — the function is healthy, it just doesn't have a default GET response. Low priority.

### 4. `/api/publish` — Returned 401 instead of 405
- **Response body:** `{"error":"unauthorized"}`
- **Assessment:** The function checks auth before checking method. This means it's running correctly — it just prioritizes auth validation over method validation. The function is healthy. Low priority.

### 5. `/api/aff` — Returned 400 instead of 302/200
- **Response body:** `{"error":"Unknown partner"}`
- **Assessment:** The affiliate redirect endpoint requires a partner parameter (e.g., `?ref=partner_code`). Without it, it correctly returns a 400 error. The function is healthy. Low priority.

---

## Health Check Details

The `/api/health` endpoint returned full system status:
- **Supabase:** ✅ OK (profiles: 0)
- **Redis:** ✅ OK (total_songs: 3)
- **Crypto:** ✅ OK
- **Node version:** v22.22.0
- **ENV vars:** All present except `SUPABASE_SERVICE_KEY` (shows `false`)

⚠️ **Note:** `SUPABASE_SERVICE_KEY` is reported as `false` in the health check. This could be the root cause of the `/api/analytics` and `/api/admin` timeouts if those endpoints rely on the service key for privileged database queries.

---

## Founding Tier Status (from `/api/founding`)
- Tier 1 (Early Founder): 500 spots, 0 claimed, $5/mo
- Tier 2 (Founding Member): 1500 spots, 0 claimed, $9.99/mo (not yet active)
- Current active tier: 1

---

*Report generated by SONIQ Endpoint Monitor — automated scheduled task*
