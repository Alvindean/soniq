{
  "name": "soniq",
  "version": "1.2.1",
  "description": "SONIQ — AI Song Creation Studio",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.0",
    "@vercel/analytics": "^2.0.1",
    "stripe": "^14.25.0"
  }
}
